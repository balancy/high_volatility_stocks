import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    container: {
        paddingTop: 12,
        paddingLeft: 0,
        paddingRight: 0,
    },
    image: {
        marginBottom: 18,
        marginRight: 24,
        border: "1px solid",
    },
});