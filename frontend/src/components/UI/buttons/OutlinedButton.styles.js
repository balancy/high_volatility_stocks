import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    button: {
        minWidth: 200,
        border: `2px solid`,
        marginRight: 12,
        marginLeft: 0,
        variant: `contained`,
    },
});