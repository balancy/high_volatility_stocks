import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    red: {
        color: `red`,
        fontWeight: 500,
    },
    green: {
        color: `green`,
        fontWeight: 500,
    },
    normal: {
        fontWeight: 400,
    }
});