import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    table: {
        marginTop: 12,
        marginBottom: 12,
    },
    tableHeader: {
        backgroundColor: `black`,
        color: `white`,
        fontSize: 12,
        fontWeight: `bold`,
    },
    tableBody: {

    },
});