import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    appBar: {
        border: `none`,
        boxShadow: `none`,
        backgroundColor: `white`,
    },
    toolBar: {
        padding: 0,
        marginBottom: 12,
    },
    space: {
        width: 12,
    }
});